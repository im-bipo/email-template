<%
	const sd = siteData || {};
	const site = sd.site || {};
	const notifications = sd.notifications || {};
	const name = site.siteName || site.name || '-';
	const phone = site.sitePhone || site.phone || '';
	const email = site.siteEmail || site.email || '';
	const footerMessage = notifications.footerMessage || '';
	const poweredBy = sd.poweredBy || 'eSrijana';
%>
			<div style="width: 100%; margin:0 auto; border-top:1px solid #f0f1f3; padding-top: 10px; padding:22px 20px 8px;">
			
				<div style="font-size:14px; color:#6b7280; margin-bottom:8px ;">Thank you for choosing our service.</div>
						<div style="font-size:14px; color:#374151; font-weight:700; margin-bottom:8px;"><%= name %></div>
						<div style="font-size:14px; color:#4b5563; margin-bottom:14px;">
							<% if (phone) { %>
								<span><%= phone %></span>
							<% } %>
							<% if (phone && email) { %>
								<span style="color:#e6e9ee; margin:0 8px;">|</span>
							<% } %>
							<% if (email) { %>
								<span><a href="mailto:<%= email %>" style="color:#4b5563; text-decoration:none;"><%= email %></a></span>
							<% } %>
						</div>
						<div style="font-size:12.2px; text-align: center; padding-block: 20px;color:#9ca3af;">© <%= new Date().getFullYear() %> Powered by <%= poweredBy %></div>
					</div>
			</div>